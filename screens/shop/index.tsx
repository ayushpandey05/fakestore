import React from 'react';
import {Text, View} from 'react-native';
import {colors} from '../../theme/colors';

const Shop = () => {
  return (
    <View>
      <Text style={{color: colors.ordinaryText}}>This is Shop screen</Text>
    </View>
  );
};

export default Shop;
