import React from 'react';
import {Text, View} from 'react-native';
import { colors } from '../../theme/colors';

const Profile = () => {
  return (
    <View>
      <Text style={{color: colors.ordinaryText}} >This is Profile screen</Text>
    </View>
  );
};

export default Profile;
